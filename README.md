# Deutsch Ecke reviews

Written automatically by the reviews relay (worker/reviews-worker.js on main).
Each entry: s = stars 1-5, r = reaction, t = comment (max 20 words), d = date.
Nothing about the visitor is stored.
